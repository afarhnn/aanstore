name: aanstore_flutter
description: Frontend scaffold for AanStore
version: 0.1.0
environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.0
  provider: ^6.0.0
  flutter_svg: ^1.1.6

flutter:
  uses-material-design: true
  assets:
    - assets/logo.svg
